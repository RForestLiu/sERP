# sERP 项目交接文档

> 最后更新: 2026-05-19
> 当前 Git HEAD: `ai/session` 分支 (68 commits ahead of main)

---

## 一、项目结构

```
g:\sERP/
├── main.py                  # ★ 项目入口（唯一启动点）
├── app.py                   # Flask 主后端（所有 API 逻辑）
├── collector.py             # 产品采集模块
├── gen_batches.py           # 生成按大类划分的翻译批次文件（一次性脚本）
├── gen_translation_txt.py   # 生成待翻译内容文本（一次性脚本）
├── count_categories.py      # 品类统计（一次性脚本）
├── requirements.txt         # Python 依赖
├── .env                     # 环境变量（API_KEY, DEEPSEEK_API_KEY 等）

├── templates/
│   ├── index.html           # 主页面（图片批量处理工作台）
│   └── ozon_listing.html    # Ozon 上架页面

├── data/
│   ├── tasks.json           # 图片处理任务列表
│   ├── products.json        # 正式产品数据（含 SKC/SKU）
│   ├── stores.json          # 店铺列表及 Ozon 凭证
│   ├── collect_tasks.json   # 采集任务持久化
│   ├── ozon_cache/          # Ozon API 缓存
│   │   ├── {store}_category_tree.json          # 品类树缓存
│   │   ├── {store}_translations.json           # 品类翻译缓存
│   │   └── {store}_attr_desc_translations.json # 属性描述翻译缓存
│   ├── task_*/              # 图片处理任务数据
│   └── collect_collect_*/   # 采集任务数据（含图片）

├── docs/
│   ├── 交接文档.md           # ← 本文档
│   ├── 需求文档.md           # 详细需求文档
│   ├── SKC-SKU规范.md        # SKC/SKU 命名规范
│   └── Ozon_Seller_API知识库.md  # Ozon API 参考

├── reference/
│   └── 店小秘--添加Ozon产品.html  # 店小秘页面参考

├── extensions/
│   └── amazon_collector/     # ★ Chrome 扩展 (Manifest V3) — 浏览器端核心
│       ├── manifest.json     # 扩展声明 (v3.2.7)
│       ├── background.js     # Service Worker — fetch 代理绕过 CSP + 消息路由
│       ├── content.js        # Amazon/1688/WB/Ozon 多平台采集 + WB 导航变体遍历
│       └── dianxiaomi_ozon.js # 店小秘 Ozon 智能上架（选品+分类+AI填充+提示词面板）

├── scripts/
│   └── dianxiaomi_autofill.user.js  # 店小秘自动填充油猴脚本（已废弃，功能已迁移到扩展）

├── 品类翻译_按大类/         # 按 25 个大类拆分的翻译批次
│   ├── batch_01_17027487_Товары для животных.txt
│   ├── batch_02_17027485_Хобби и творчество.txt
│   └── ... (共 25 个文件)

└── test_images/             # 测试图片
```

---

## 二、启动方式

### 2.1 后端启动

```bash
cd g:\sERP
python main.py
# 访问 http://localhost:5000
```

### 2.2 浏览器扩展加载

1. 打开 Chrome → `chrome://extensions/`
2. 开启「开发者模式」
3. 点击「加载已解压的扩展程序」→ 选择 `g:\sERP\extensions\amazon_collector\`
4. 扩展 ID 需固定，确保 `chrome.storage` 数据不丢失

### 2.3 环境变量

`.env` 文件需配置（参考 `.env.example`）：
- `DEEPSEEK_API_KEY` — DeepSeek API 密钥（品类翻译、品类匹配、属性/表单填充）
- `API_KEY` — 图片生成 API 密钥（Gemini 系列）

---

## 三、模块架构

### 模块关系图

```
main.py (入口)
   │
   ├── app.py (Flask 后端) ← 所有 HTTP API 和路由
   │      │
   │      ├── 图片批量处理 (task_* 数据目录)
   │      ├── 产品采集 (/api/collect/* + /api/browser-capture)
   │      ├── 产品管理 (/api/products/*)
   │      ├── Ozon 品类 (/api/ozon/*)
   │      │     ├── 品类树 (category-tree + 缓存)
   │      │     ├── 品类翻译 (translate-categories)
   │      │     ├── 品类匹配 (match-category → DeepSeek)
   │      │     └── 品类属性 (category-attributes)
   │      ├── Ozon 上架草稿 (/api/listings/*)
   │      ├── 店小秘自动填充 (/api/auto-fill/*)
   │      └── 店铺管理 (/api/stores/*)
   │
   ├── collector.py (采集引擎)
   │      ├── 抓取层: Playwright + requests 双引擎
   │      ├── 决策层: DeepSeek 图片分类
   │      └── 执行层: aiohttp 异步下载 + Pillow 转 JPG
   │
   └── extensions/amazon_collector/ (Chrome 扩展)
          ├── content.js → Amazon/WB/1688 页面采集
          ├── dianxiaomi_ozon.js → 店小秘 Ozon 智能上架
          └── background.js → fetch 代理 (绕过 CSP)
```

### 关键数据流

```
Amazon 采集流程:
  浏览器打开 Amazon 产品页
  → content.js 提取标题/价格/属性/描述/图片URL
  → bgFetch → POST /api/browser-capture
  → 后端存储 product_data.json (不含图片URL)
  → 后台线程下载图片

店小秘自动填充流程:
  用户在店小秘打开 Ozon 产品添加页
  → dianxiaomi_ozon.js 注入左侧工具栏
  → 选品 → 智能分类 → 自动填充
  → collectFormFields() 扫描表单字段
  → 分批发送 POST /api/auto-fill/analyze (DeepSeek)
  → fillFormField() 逐个填入 (绕过 Vue v-model 受控组件)
  → 结果面板显示填充状态

品类树翻译流程:
  Ozon API → category_tree.json (缓存)
           → 展平 7998 节点
           → 逐批发 DeepSeek → translations.json (缓存)
           → 前端合并渲染

Ozon 上架流程:
  产品数据 + 品类树 → DeepSeek 匹配品类
                    → 获取品类属性
                    → DeepSeek 填充属性值
                    → 保存上架草稿
```

### 浏览器扩展架构详解

#### manifest.json
- **权限**: `storage` (chrome.storage.local 持久化)
- **host_permissions**: `localhost:5000` / `127.0.0.1:5000`
- **content_scripts**: 匹配 Amazon/WB/1688/Ozon/dianxiaomi.com 页面
- **background**: Service Worker 代理 fetch 请求 (绕过店铺页面的 CSP 限制)

#### dianxiaomi_ozon.js — 核心模块
| 功能 | 实现 |
|------|------|
| 左侧工具栏 | 3 按钮: 选品 → 分品类 → 自动填充 |
| 产品选择弹窗 | 搜索 + 列表 + SKC/标题展示 |
| 品类自动匹配 | 调用后端 → DeepSeek → Ant Design 级联选择器逐层填入 |
| 表单收集 | 扫描所有 input/select/textarea，跳过店铺/分类字段 |
| 分批填充 | 关键字段 (标题/描述/标签/JSON) 首批，其余每批 5 个 |
| Vue 组件填充 | focus→clear→input→change 绕过 v-model 受控逻辑 |
| checkbox/radio | 优先 boolean，否则标签文本匹配 |
| JSON 编辑器 | 检测 .ant-switch → 自动切换至代码模式再填入 |
| 额外提示词面板 | 3 层: 平台/店铺/品类，独立保存，chrome.storage 持久化 |
| 结果面板 | 悬浮列表展示每字段填充状态 (✅/❌) |

#### 额外提示词三层模型
```
平台提示词 (ozon/amazon/wb)
  ├── 产品标题填充提示
  ├── 产品描述填充提示
  ├── JSON富文本填充提示
  ├── 主题标签填充提示
  └── 平台专属提示 (如"用俄语填写")

店铺提示词 (ozon_anling/ozon_anmei/...)
  └── 店铺专属填充指引

品类提示词 (店铺+品类都有时才显示)
  └── 品类专属填充指引
```

chrome.storage key 设计:
- `serp_hint_platform_{platform}` → `{ title, description, json_text, hashtag, platform_prompt }`
- `serp_hint_store_{storeId}` → `{ prompt }`
- `serp_hint_category_{storeId}_{categoryName}` → `{ prompt }`

---

## 四、日志系统

### 4.1 架构

| 文件 | Logger 名称 | 说明 |
|------|-------------|------|
| `app.py` | `logging.config.dictConfig` | ★ 唯一日志配置入口，在模块顶层执行 |
| `app.py` | `__name__` → `app` | 所有 API 路由日志 |
| `collector.py` | `__name__` → `collector` | 采集引擎日志 |
| `main.py` | — | 仅加载 .env + 启动 Flask，不配置日志 |

### 4.2 日志格式

```
HH:MM:SS | INFO  | app | 消息内容
HH:MM:SS | WARN  | app | 警告内容
HH:MM:SS | ERROR | app | 错误内容
HH:MM:SS | DEBUG | app | 调试内容（品类匹配等）
```

### 4.3 关键设计决策

- **为什么 `dictConfig` 放在 `app.py` 顶部而不是 `main.py`？**
  Flask `debug=True` 的 reloader 在 Windows 上通过 `subprocess.Popen` 创建子进程，子进程不会执行 `main.py` 顶层代码。放在 `app.py` 顶部保证无论哪个进程导入都能生效。
- **为什么需要 `sys.stdout.reconfigure(encoding='utf-8')`？**
  Windows 终端默认 GBK 编码无法输出 ❌ ✅ 等 emoji，会导致 `UnicodeEncodeError`，日志消息被静默丢弃。
- **`disable_existing_loggers: False`** — 防止 Flask/Werkzeug 已有的 logger 被禁用

### 4.4 改造记录（2026-04-27）

- `app.py`: 删除约 60 处 `print()`，改为 `logger.info/warning/error/debug`；删除 `app.run()`
- `collector.py`: 删除约 11 处 `print()`，改为 `logger`；修复文件结构损坏问题
- 新建 `main.py`: 统一初始化日志配置和 `.env` 加载，作为唯一入口

### 4.5 改造记录（2026-04-27 第二次）

- `app.py`: 品类翻译后台任务由串行改为并发（`ThreadPoolExecutor` max_workers=3），每批加入 1-2s 随机抖动缓冲
- `app.py`: 全部 DeepSeek JSON 解析逻辑从 `re.search(r'\{[^{}]*\}')` 替换为清理 markdown 代码块包裹后直接 `json.loads()`，涉及 3 处
- `app.py`: 每个线程使用本地 dict 收集翻译结果，通过锁合并到全局，避免并发写入冲突

### 4.6 改造记录（2026-04-29）

- **日志配置迁移到 `app.py`**：删除 `main.py` 中的 `logging.basicConfig`，改为 `app.py` 顶部 `logging.config.dictConfig`
- **UTF-8 编码修复**：`sys.stdout/stderr.reconfigure(encoding='utf-8')` 解决 Windows GBK 编码导致 emoji 日志丢失的 bug（根因：`UnicodeEncodeError: 'gbk' codec can't encode character '❌'`）
- **属性描述翻译**：新增 `_translate_attr_descriptions` + 缓存 `{store_id}_attr_desc_translations.json`，在品类属性 API 中自动翻译俄语属性描述为中文
- **字典值 API 修复**：`/v1/description-category/attribute/values` 调用始终传递 `type_id`，新增详细诊断日志
- **前端日志增强**：自动匹配和 LLM 填充显示耗时和失败原因

### 4.7 改造记录（2026-04-29 第二次 — UI 对标店小秘重构）

**对标分析**：对比店小秘 `添加Ozon产品` 页面截图，识别差异并优化。

**改动清单**：

| 模块 | 改动 | 说明 |
|------|------|------|
| 描述区 | textarea | 纯文本编辑 + HTML 预览切换按钮，可预览 AI 填充后的富文本效果 |
| 图片上传 | 轮播 → 网格拖拽 | 虚线框拖拽区 + SortableJS 排序 + Cropper.js 裁剪 |
| SKU 表格 | 新增 | 变种信息表格：图片/名称/价格/库存/条码/重量 + 批量设置工具栏 |
| 顶部导航栏 | sticky 置顶 | position:sticky 固定显示，类似 Excel 冻结首行 |
| 店铺选择 | URL参数 → 下拉 | 页面内切换店铺，自动重新加载 |
| 属性布局 | 单列 → 宽屏2列 | ≥1100px 时 grid 2 列排列 |
| 属性标签 | 中文为主 → 俄语(中文) | 行内双语，加序号，单位移入输入框右侧 |
| 提示横幅 | 无 → 黄色警告条 | 品类属性区顶部提示 Ozon 平台规则 |
| 产品摘要栏 | 无 → 顶部横条 | 显示缩略图 + SKC + 中英文标题 |

**新增 CDN 依赖**：
- SortableJS 1.15.0 — 图片拖拽排序
- Cropper.js 1.5.13 — 图片裁剪

**API 兼容**：`/api/stores` 同时兼容列表和 `{stores:[...]}` 两种返回格式。

### 4.8 改造记录（2026-04-29 第三次 — Bug 修复 + 变种添加）

**Bug 修复：所有按钮失效**
- **根因**：`renderCarouselImages()` 函数体引用了已删除的 DOM 元素 `carouselArea` 和 `carouselAddBtn`，`ReferenceError` 发生在顶层脚本解析阶段，导致所有事件监听器注册失败
- **修复**：清除旧函数体，替换为空 stub；移除调试标记（绿色边框、window.onerror）

**新增功能：变种添加**
- SKU 变种信息卡片标题栏新增 `+ 添加变种` 按钮
- 提取 `_createSkuRow(skuCode, initialData)` 工厂函数，产品 SKU 和手动变种共用
- 手动添加的变种名称可编辑，产品自带 SKU 名称只读
- `saveDraft()` 自动收集所有 SKU 行数据到 `listingDraft.sku_data` + `listingDraft.custom_skus`
- `restoreDraft()` 自动还原上次的变种数据
- Quill.js CDN 依赖已移除（改用 textarea + HTML 预览切换）

### 4.9 改造记录（2026-05-01 ~ 05-09 — Chrome 扩展 + 自动填充迭代）

**Chrome 扩展 — 店小秘 Ozon 智能上架**:

| 日期 | 改动 | 说明 |
|------|------|------|
| 05-01 | 品类选择重写 | 逐层树导航 + 轮询等待 + 多策略匹配 + Ant Design Modal 级联面板 |
| 05-01 | 品类型号修复 | 清除按钮导致无限递归 → 防重入锁 |
| 05-02 | 品类策略模式 | 按平台分派填充策略（Ozon/Amazon/WB/1688），便于多平台扩展 |
| 05-03 | 分批填充 | 表单字段太多时批量发送 DeepSeek 分析，避免单次 token 溢出 |
| 05-04 | 额外提示词面板 | 平台/店铺两层，chrome.storage 持久化 |
| 05-04 | 主题标签字段 | 提示词面板新增 #hashtag 输入 |
| 05-05 | 填充逻辑修复 | 先清空再填入 + 结果面板按页面顺序 + 避免遮挡 |
| 05-06 | 三层提示词重构 | 平台/店铺/品类三层，每层独立保存按钮 |
| 05-07 | 关键字段优先批 | 标题/描述/标签/JSON 首批发送（更高质量注意力） |
| 05-07 | checkbox 匹配 | 非 boolean 值时匹配选项标签文本 |
| 05-07 | JSON 编辑器开关 | 自动检测并点击"编辑JSON代码"切换 |
| 05-08 | LLM 上下文增强 | 发送 brand/category/rating/product_details/variants 到 LLM |
| 05-08 | prompt 放宽 | "不确定跳过" → "能推断就填"，属性类字段尽量填充 |
| 05-09 | 批次缩小 | 非关键字段每批 10 → 5，提升返回质量 |

**Chrome 扩展 — Amazon 采集修复**:

| 日期 | 改动 | 说明 |
|------|------|------|
| 05-03 | About this item | 废弃 `#feature-bullets` → 使用新版 `#pqv-feature-bullets` |
| 05-03 | product_details | 从纯文本改为结构化 JSON (key:value 行解析) |
| 05-03 | 去重 | 删除重复的 `extractAboutItem`，由 bullets.join 派生 |
| 05-04 | 图片 URL 剥离 | 存储时移除图片 URL (仅下载线程持有)，减少 JSON 体积 |
| 05-04 | 全量图片采集 | 移除 `.slice(0,3)` 限制，主图全部采集 |
| 05-05 | 描述清洗 | 过滤 "Previous page / Next page" 翻页文本，阈值 30→50 字符 |

**Chrome 扩展 — Wildberries (WB) 采集器**:

| 日期 | 改动 | 说明 |
|------|------|------|
| 05-12 | 弹窗描述提取 | 点击"Характеристики и описание"按钮打开弹窗，提取 descriptionText |
| 05-12 | product_details | 弹窗内 data-testid 表格 th/td 规格参数提取 |
| 05-12 | 变体遍历 | fetchWBVariantData 全量图片采集（扩展CDN域名 wbcontent.net） |
| 05-13 | 精确选择器净化 | extractVariants 改用 `a[data-nm-id]` 排除推荐商品卡片 |
| 05-13 | 图片产品过滤 | extractImages 按 productId 过滤 CDN URL，排除推荐商品图片 |
| 05-13 | 价格修正 | 优先 `.price--Xne45`（弹窗真实价），备选 `.priceWrap--pxdH1` |
| 05-13 | 去超链接 | buildPayload 移除 variants.urls（内部导航数据不发送服务端） |
| 05-19 | 导航式变体遍历 | 全部规格改为实际导航到变体页面，从 live DOM 采集（替代 fetch HTML） |
| 05-19 | 遍历状态机 | chrome.storage.local 跨页面状态传递，重试机制等待页面水合 |
| 05-19 | CSS Module 防失效 | 按钮/描述/价格选择器改为多级 fallback（class → text → attr），防止 hash 轮换 |
| 05-19 | 价格 fallback | `[class*=\"price--\"]` 模糊匹配替代不存在的 `meta[itemprop]` |
| 05-19 | Floating UI drawer | 单采添加轮询重试（12次×250ms），等待 Floating UI portal 异步渲染 |

---

## 五、当前已知问题

### 🟡 问题 1：SKU 多行相同标签字段被重复填充不同值

**文件**: `extensions/amazon_collector/dianxiaomi_ozon.js` → `collectFormFields()`

**症状**:
- Ozon 产品表单有多行 SKU/变体，每行都有相同标签的字段（如"统一计量单位中的商品数量"）
- 当前 `collectFormFields()` 收集的字段标签完全相同，LLM 无法区分属于哪个 SKU 行
- 导致同一字段在不同行被填入随机不同值

**根因**: `findLabel()` 只读取 Ant Design form-item label，不感知所在 SKU 行上下文

**修复方向**: 检测字段所在 table row，将 SKU 名称/颜色等变体信息附加到 label 中发给 LLM

### 🟡 问题 2：额外提示词未在自动填充时生效 (已定位，待修复)

**文件**: `dianxiaomi_ozon.js` → `collectCustomPrompts()`

**症状**:
- 用户保存了俄语提示词，但自动填充结果仍是英文
- `collectCustomPrompts()` 从 DOM 元素读取值，但 DOM 元素仅在面板打开时从 chrome.storage 恢复

**根因**: 页面刷新后 `loadAllHints()` 在 init 时调用，但此时 `detectStoreId()` 可能因页面未完全加载而返回 null，导致提示词未被加载到 DOM。后续 `collectCustomPrompts()` 读到空值

**修复方向**: `collectCustomPrompts()` 改为直接从 chrome.storage.local 读取，不依赖 DOM 状态

### 🟡 问题 3：单位信息未传递给 LLM

**症状**: Amazon 使用盎司 (oz)、英寸 (in)、磅 (lb) 等单位，Ozon 使用克 (g)、厘米 (cm)。LLM 填充重量/尺寸时缺少单位换算上下文

**修复方向**: 在 `product_texts` 中追加单位信息，或在 system_prompt 中添加换算规则

### 🟡 问题 4：品类翻译偶有解析失败

**文件**: `app.py` → `_translate_categories_batch()`（ThreadPoolExecutor 并发翻译）

**症状**: 偶发某些批次 `✅ 成功解析 0 个翻译结果`；DeepSeek 返回格式偶有波动

**当前状态**: 已清理 markdown 代码块再 `json.loads()`，大部分正常。仍有少数因输出截断导致解析失败。

### 🟡 问题 5：品类匹配 DeepSeek max_tokens 偏小

**文件**: `app.py` → `ozon_match_category`，`max_tokens: 512`

当前品类匹配仅返回品类 ID，512 tokens 足够。如需返回更多属性建议，可调大到 1024。

### 🟡 问题 6：WB 选择器强依赖 CSS Module hash — 每次构建可能失效

**文件**: `extensions/amazon_collector/content.js` → WB EXTRACTORS

**症状**:
- WB 使用 CSS Modules，类名后缀（`--nrkiv`, `--JBcnf`, `--Xne45`）每次前端构建都会变
- 当前已添加 text content 和 attribute fallback，但 fallback 也有局限：
  - `_findByText(["Характеристики", "описание"])` — 扫描全页 button/span/div，O(N) 可能误匹配
  - `_findLongTextInDrawer()` — 依赖 `.mo-drawer__paper` 容器在 DOM 中，Floating UI 关闭时不存在
  - `[class*=\"price--\"]` — 同样依赖 CSS Module 前缀不变

**当前缓解**: 3 个关键选择器各有多级 fallback。线上功能目前正常，但下次 WB 构建后可能需要再次更新 class 前缀。

**根治方向**: 用 `data-testid` 属性（WB 已稳定使用）替代所有 CSS class 选择器；或改用子资源完整性哈希检测构建变更。

### 🟡 问题 7：WB 单采 Floating UI drawer — 轮询重试

**文件**: `extensions/amazon_collector/content.js` → `collectCurrent()`

**症状**:
- Floating UI portal 在 React hydration 后关闭状态时 drawer content 不在 DOM 中
- `_ensureDrawerOpen()` 点击按钮后 React 18 异步批处理渲染，可能数百毫秒后才出现内容
- 当前已添加轮询重试（12 次 × 250ms = 3s），正常网络下足够
- 极端情况下（页面卡顿、慢网络）3 秒可能不够；且轮询是盲等，不够高效

**修复方向**: 改用 MutationObserver 监听 `.mo-drawer__paper` 或 `.descriptionText--JBcnf` 出现在 DOM 中，替代盲等轮询

### 🟢 已修复：品类匹配截断问题

删除了 `MAX_TREE_NODES = 2000` 截断逻辑，现在全量 7998 个品类发送给 DeepSeek。

### 🟢 已修复：字典值下拉列表不显示

`/v1/description-category/attribute/values` 调用始终传递 `type_id`。

### 🟢 已修复：属性描述为俄语

新增 `_translate_attr_descriptions` + 缓存，自动翻译属性描述为中文。

---

## 六、API 路由总览

| 路由 | 功能 | 函数 |
|------|------|------|
| `GET /` | 图片批量处理主页 | `index` |
| `GET /ozon-listing` | Ozon 上架页面 | `ozon_listing_page` |
| `GET /api/ozon/<id>/category-tree` | 品类树 | `ozon_category_tree` |
| `POST /api/ozon/<id>/refresh-categories` | 更新品类树+翻译 | `ozon_refresh_categories` |
| `POST /api/ozon/<id>/match-category` | 品类匹配 | `ozon_match_category` |
| `POST /api/ozon/<id>/translate-categories` | 品类翻译 | `ozon_translate_categories` |
| `POST /api/ozon/<id>/category-attributes` | 品类属性 | `ozon_category_attributes` |
| `POST /api/collect` | 启动采集 | `start_collect` |
| `POST /api/browser-capture` | 浏览器端采集数据接收 | `browser_capture` |
| `GET /api/products` | 产品列表 | `get_products` |
| `POST /api/auto-fill/analyze` | 店小秘填充分析 (DeepSeek) | `auto_fill_analyze` |
| `POST /api/auto-fill/ozon-fields` | Ozon 属性填充 | `auto_fill_ozon_fields` |

### auto_fill_analyze 请求/响应

```json
// 请求
{
  "skc": "WALLET-0001",
  "product_title": "产品标题",
  "product_data": { /* product_data.json 全部内容 */ },
  "manual_data": { /* 人工登记数据 */ },
  "form_fields": [
    {
      "tag": "input|select|textarea",
      "type": "text|checkbox|radio|...",
      "label": "表单字段中文标签",
      "placeholder": "占位文本",
      "currentValue": "当前值",
      "options": ["下拉选项..."],
      "selector": "CSS 选择器"
    }
  ],
  "custom_prompts": {
    "title": "标题填充提示",
    "description": "描述填充提示",
    "json_text": "JSON填充提示",
    "hashtag": "主题标签提示",
    "platform": "平台专属提示 (如: 用俄语)",
    "store": "店铺提示",
    "category": "品类提示"
  }
}

// 响应
{
  "mappings": [
    { "selector": "CSS选择器", "value": "填充值" }
  ]
}
```

---

## 七、品类树缓存工作原理

```
Ozon API → 品类树 (7998 节点)
         → category_tree.json (原始缓存)
         → 展平所有节点
         → 检查 translations.json (id → 中文)
         → 未命中 → 分批发送 DeepSeek 翻译
         → 写入 translations.json
         → 前端合并渲染

关键文件:
  data/ozon_cache/{store_id}_category_tree.json
  data/ozon_cache/{store_id}_translations.json
```

---

## 八、开发指南

### 8.1 启动调试

```bash
cd g:\sERP
python main.py
# 会在终端输出所有日志
```

### 8.2 扩展调试

1. Chrome `chrome://extensions/` → 找到 sERP ExtensionHelper → 点击 "service worker" 链接可调试 background.js
2. 在店小秘页面 F12 → Sources → Content Scripts → 找到 dianxiaomi_ozon.js 设置断点
3. chrome.storage 数据查看: Console → `chrome.storage.local.get(null, d => console.log(d))`

### 8.3 代码规范

- 新增日志请使用 `logger.info/warning/error/debug`，不用 `print()`
- 品类的调试日志用 `logger.debug()` 级别，避免生产环境干扰
- `main.py` 是唯一启动入口，不要直接运行 `app.py`
- 扩展代码使用 IIFE + `"use strict"` 包裹，避免全局变量污染

### 8.4 当前分支

- 开发在 `ai/session` 分支 (68 commits ahead of main)
- 主分支为 `main`（稳定版）
- 尚未合并到 `main`

### 8.5 未实现的功能

- **导航栏模块切换**：`index.html` 中已有导航栏结构（`<div class="nav-bar">`），但只有"采集产品""图片批量处理""产品管理"菜单项，模块切换功能未完整实现
- **SKU 行上下文感知填充**：多行 SKU 字段需附加上下文区分（问题 1）

### 8.6 Claude Code 配置

`.claude/settings.json` — 项目级自动审批权限：
- 自动批准 Read / Edit / Write / Glob / Grep / WebFetch / WebSearch
- 自动批准 Bash 操作（git、npm、pip、python、node、curl）
- 仅拒绝 `rm -rf` 高危操作

`.claude/settings.local.json` — 本地环境变量（API Key）：
- ANTHROPIC_BASE_URL / ANTHROPIC_AUTH_TOKEN / ANTHROPIC_MODEL
- 已加入 .gitignore，不会提交到仓库

`CLAUDE.md` — Claude Code 行为规范（代码风格、变更原则等）
